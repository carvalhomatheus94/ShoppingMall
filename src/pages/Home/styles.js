import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  wrapBanner: {
    height: 250,
  },
  scrollHorizontal: {
    marginTop: 30
  },
});

export default styles;
